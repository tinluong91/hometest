import { seed } from './s-user'
console.log('Seeding database...')  
/**
 * Seed the server with mock data for testing purposes. There should be at least:
 * - 10 Users
 * - 3 Posts per User
 * - 5 Comments per Post
 */
void seed()
